<div class="row justify-content-center align-items-center vh-100" id="backdrop">

  <div class="dash-card form-card mt-5">

    <h2>Your company</h2>
    <p>Tell us about your company. Enter the following elements accuratly and we will try to do the job for you by fetchign the rest online.</p>


    <%= form_with model: @company, local: true do |form| %>
      <div class="form-group mt-5">
        <%= form.label :name %>
        <%= form.text_field :name, class: "form-control" %>
      </div>

      <div class="form-group mt-5">
        <%= form.label :domain %>
        <%= form.text_field :domain, class: "form-control" %>
      </div>

      <div class="form-group mt-5">
        <%= form.label :country %>
        <%= form.text_field :country, class: "form-control" %>
      </div>

      <%= form.submit "Create Company", class: "btn btn-primary mt-5" %>
    <% end %>
  </div>

</div>
